<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Include EditTexts for email/phone number and other required information -->
    <EditText
        android:id="@+id/et_full_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:hint="Full Name"
        android:inputType="text" />

    <EditText
        android:id="@+id/et_email_or_phone_signup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/et_full_name"
        android:layout_marginTop="16dp"
        android:hint="Email or Phone Number"
        android:inputType="text" />

    <!-- Include Signup button -->
    <Button
        android:id="@+id/signup_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/et_email_or_phone_signup"
        android:layout_marginTop="24dp"
        android:text="Signup" />
</RelativeLayout>
